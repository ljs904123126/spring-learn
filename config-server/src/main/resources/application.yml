spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ljs904123126/config-repo.git
          username:
          password:
          basedir: D:/Config/basedir
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/